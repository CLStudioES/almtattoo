---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<button
    class="px-4 py-2 text-white bg-otoño-darkGreen hover:scale-110 transition-transform duration-105 tracking-wider"
    >{title}</button
>
